# Проверка обновления от 9 сентября 2026

Рабочая папка: `yandex_game`. Файлы пользователя в Downloads не перезаписывались; существующие локальные сохранения браузера не использовались для тестов.

## Выполнено

- `npm test`: 118 тестов, 5 файлов — успешно.
- `npm run build`: TypeScript и Vite production build — успешно.
- `git diff --check`: успешно.
- `scripts/visual-smoke.mjs`: отдельный временный профиль Chrome, реальные DOM/Canvas и ввод, тестовые сохранения, подставной Yandex SDK.
- Клик и drag-and-drop мышью, недопустимое размещение у границы, сохранение и восстановление после перезагрузки.
- Сенсорное касание и перетаскивание через touch events Chromium; размещение клавиатурой.
- Цветение, роса, секатор, очистка линии; сохранённый цвет фигур после установки.
- SDK pause/resume вместе с открытой паузой меню; возобновление SDK не отменяет меню.
- Ошибка rewarded-видео сохраняет забег; успешная награда даёт ровно одно продолжение; кнопка защищена от повторного запроса.
- Отмена авторизации не блокирует игру.
- 1536×1024, 1366×768, 390×844, 320×568 и 844×390: Canvas и управление доступны без браузерной прокрутки.
- RU и неподдерживаемый язык (`de-DE`), который корректно переключает весь интерфейс на EN.
- Reduced motion: отсутствуют частицы и крупные движущиеся вспышки.
- В финальном браузерном прогоне ошибок JavaScript не было.

Скриншоты находятся в `artifacts/qa/`. Это технические снимки настоящего рендера с тестовыми состояниями, не готовые промоматериалы и не результаты живых игроков.

## Не подтверждено локальными тестами

Настоящая реклама, настройки SDK в черновике, Game Ready debug-индикатор, межустройственная облачная синхронизация, реальный лидерборд и модерация. Нужны проверки внутри Консоли Яндекс Игр. Реальные iPhone/Android и Safari/Firefox в этом прогоне не использовались: сенсорный тест выполнен в Chromium.

Изменения не публиковались в Яндекс Игры и не отправлялись на GitHub в ходе этого обновления.
